# live-build generated state and build artifacts
.build/
binary/
binary.hybrid.iso
chroot/
cache/

# live-build writes generated config stanzas into config/ after `lb config`
config/binary
config/bootstrap
config/chroot
config/common
config/source

# Generated by config/auto/config based on $LINUS_REPO_URL
config/archives/linus.list.chroot

# Built ISO (kept in dist/ but not tracked)
dist/*.iso

# Rust build
target/

# Editor
.idea/
.vscode/
*.swp
*.swo
