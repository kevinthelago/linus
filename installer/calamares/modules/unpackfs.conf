---
# Copy the live squashfs to the target partition (offline install — no network required).
# The squashfs is already mounted by live-boot at /run/live/rootfs/filesystem.squashfs.
unpack:
    -   source:      /run/live/rootfs/filesystem.squashfs
        sourcefs:    squashfs
        destination: ""
