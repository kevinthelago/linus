---
# Generates /crypto_keyfile.bin in the target and embeds it in the initramfs so that
# the user is prompted for the passphrase only once at boot (unlocks root + swap together).
# This module is a no-op when no LUKS partition was created in the partition step.
