[package]
name = "smoke-boot"
version = "0.1.0"
edition = "2021"
description = "QEMU smoke-boot harness for Linus ISO verification"

[[bin]]
name = "smoke-boot"
path = "src/main.rs"

[dependencies]
anyhow = "1"
serde_json = "1"
clap = { version = "4", features = ["derive"] }
