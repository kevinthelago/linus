# /etc/systemd/system/greetd.service.d/linus.conf
# Installed by linus-greeter. Ensures greetd restarts on crash with a brief
# cooldown so a rapid-failure loop cannot freeze the VT.

[Service]
Restart=on-failure
RestartSec=3
